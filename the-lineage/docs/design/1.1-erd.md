# 1.1 — Entity Relationship Diagram

> **Marker legend.** `PK` = primary key, `FK` = foreign key, `UK` = unique constraint. Cardinality uses crow's-foot notation: `||--o{` is one-to-many, `||--||` one-to-one, `||--o|` one-to-zero-or-one.

```mermaid
erDiagram
    USER ||--o| SELLER_PROFILE : "is a"
    USER ||--o{ SELLER_APPLICATION : submits
    USER ||--o| CART : owns
    USER ||--o{ ORDER : places
    USER ||--o{ COMMENT : writes
    USER ||--o{ REVIEW : writes
    USER ||--o{ NOTIFICATION : receives
    USER ||--o{ PROVENANCE_RECORD : "is party to"

    SELLER_PROFILE ||--o{ LISTING : owns
    SELLER_PROFILE ||--o{ SHOE : submits

    SELLER_APPLICATION }o--|| USER : "submitted by"
    SELLER_APPLICATION }o--o| USER : "reviewed by (curator)"

    SHOE ||--o{ PROVENANCE_RECORD : has
    SHOE ||--o| LISTING : "currently listed as"

    LISTING ||--o{ CART_ITEM : "reserved in"
    LISTING ||--o{ COMMENT : has
    LISTING ||--o| ORDER : "sold via"

    CART ||--o{ CART_ITEM : contains
    CART_ITEM }o--|| LISTING : references

    ORDER ||--|| PAYMENT : "paid by"
    ORDER ||--|| SHIPPING_RECORD : "shipped via"
    ORDER ||--o| REVIEW : "reviewed in"
    ORDER ||--o{ DISPUTE : "may have"

    COMMENT ||--o{ COMMENT : "parent of (thread)"

    USER {
        UUID id PK
        string email UK
        string password_hash
        string display_name
        enum role "BUYER|SELLER|CURATOR|ADMIN"
        timestamp created_at
        boolean active
    }

    SELLER_PROFILE {
        UUID id PK
        UUID user_id FK,UK
        enum tier "TIER_1|TIER_2|TIER_3|VERIFIED_HOUSE"
        enum application_status "NONE|PENDING|APPROVED|REJECTED"
        string bio
        string banner_url
        timestamp approved_at
    }

    SELLER_APPLICATION {
        UUID id PK
        UUID user_id FK
        UUID reviewer_id FK "curator, nullable"
        enum status "PENDING|APPROVED|REJECTED"
        string narrative
        string references_json
        string inventory_summary
        string reviewer_note
        timestamp submitted_at
        timestamp reviewed_at
    }

    SHOE {
        UUID id PK
        UUID seller_profile_id FK
        string passport_id UK
        string brand
        string model
        string colorway
        int era_year
        enum condition_grade "MINT|EXCELLENT|VERY_GOOD|GOOD|FAIR"
        int rarity_score
        UUID authenticated_by_curator_id FK "nullable"
        timestamp authenticated_at
        timestamp created_at
    }

    PROVENANCE_RECORD {
        UUID id PK
        UUID shoe_id FK
        UUID actor_user_id FK
        enum event_type "SUBMITTED|AUTHENTICATED|LISTED|SOLD|SHIPPED|RECEIVED|DISPUTED|RESOLVED|UNLISTED"
        string payload_json
        timestamp occurred_at "immutable"
    }

    LISTING {
        UUID id PK
        UUID shoe_id FK,UK
        UUID seller_profile_id FK
        decimal price
        string currency
        enum state "AVAILABLE|RESERVED|SOLD|UNLISTED"
        timestamp state_changed_at
        timestamp created_at
    }

    CART {
        UUID id PK
        UUID user_id FK,UK
        timestamp created_at
    }

    CART_ITEM {
        UUID id PK
        UUID cart_id FK
        UUID listing_id FK
        timestamp reserved_at
        timestamp expires_at
    }

    ORDER {
        UUID id PK
        UUID listing_id FK,UK
        UUID buyer_user_id FK
        UUID seller_profile_id FK
        decimal total_amount
        string currency
        enum status "CREATED|PAID|SHIPPED|DELIVERED|COMPLETED|REFUNDED|DISPUTED"
        timestamp created_at
        timestamp completed_at
    }

    PAYMENT {
        UUID id PK
        UUID order_id FK,UK
        string processor_reference
        enum payment_status "INITIATED|AUTHORIZED|CAPTURED|FAILED|REFUNDED"
        enum escrow_status "HELD|RELEASED|REVERSED"
        decimal amount
        timestamp captured_at
        timestamp escrow_released_at
    }

    COMMENT {
        UUID id PK
        UUID listing_id FK
        UUID parent_id FK "nullable, threading"
        UUID author_user_id FK
        string body
        boolean flagged
        timestamp created_at
    }

    REVIEW {
        UUID id PK
        UUID order_id FK,UK
        UUID author_user_id FK
        UUID seller_profile_id FK
        int accuracy_score "1..5"
        int condition_score "1..5"
        int shipping_score "1..5"
        string body
        timestamp created_at
    }

    NOTIFICATION {
        UUID id PK
        UUID user_id FK
        enum type "APPLICATION_UPDATE|LISTING_COMMENT|ORDER_UPDATE|PAYOUT|DISPUTE"
        string payload_json
        boolean read
        timestamp created_at
    }

    SHIPPING_RECORD {
        UUID id PK
        UUID order_id FK,UK
        string carrier
        string tracking_number
        enum status "PENDING|IN_TRANSIT|DELIVERED|LOST"
        timestamp shipped_at
        timestamp delivered_at
    }

    DISPUTE {
        UUID id PK
        UUID order_id FK
        UUID raised_by_user_id FK
        UUID resolver_user_id FK "admin, nullable"
        enum status "OPEN|UNDER_REVIEW|RESOLVED_BUYER|RESOLVED_SELLER"
        string reason
        string resolution_note
        timestamp opened_at
        timestamp resolved_at
    }
```

## Notes

- **`ProvenanceRecord`** has no `updated_at`; entries are immutable once written.
- **`Shoe` ↔ `Listing`** is 1-to-0/1 — a shoe can exist without an active listing (e.g. after sale, between listings).
- **`Order` ↔ `Listing`** is 1-to-1; a listing can be sold at most once in its lifetime.
- **`Payment.escrow_status`** is tracked independently of `payment_status` because captured funds may still be held.
- **`SellerProfile.application_status`** is a denormalized cache of the latest `SellerApplication.status` for fast gating of seller-only actions.
